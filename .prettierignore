# Dependencies and build output
node_modules
dist
dist-ssr
dist-data
storybook-static
coverage
playwright-report
test-results
.venv
venv

# Research pipeline / local-only assets
archive
books
chunks
out
summaries

# Lockfile is npm-managed
package-lock.json

# Generated data JSON — written by scripts (build-dataset, build_dictionary,
# exports), so formatting must stay byte-stable with the generators
src/data/*.json
data/
urdu-i18n/*.json

# Urdu content sources + generated log — consumed/written by the Python
# pipeline (build_urdu_content.py, update_log.py); formatting must stay
# byte-stable so regeneration produces no diff
urdu-i18n/content/
urdu-i18n/TRANSLATION_LOG.md

# Out of cleanup scope: quarantined pre-React reference app + one-off eval outputs
legacy/
eval/
