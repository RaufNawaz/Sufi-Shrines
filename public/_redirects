# Legacy slug redirects (index-suffixed → stable name-based).
# Generated by: node scripts/backfill-slugs.mjs --redirects-only
# Re-run whenever the sheet gets new explicit Slug values.
# (entries go here)

/* /index.html 200
