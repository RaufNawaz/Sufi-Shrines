# Dependencies
node_modules/
.pnp
.pnp.js
.venv/
venv/

# Build output
dist/
dist-ssr/
shrines-ocr-library-kit.zip
dist-data/
build/

# Environment variables (never commit real keys)
.env
.env.local
.env.*.local

# Large research assets (use Git LFS or external storage instead)
*.pdf
urd.traineddata
tessdata/
__pycache__/
*.pyc
out/
books/

# Research pipeline outputs — regenerable from books/ + out/, kept local
chunks/
summaries/

# Working/planning files — local only, not part of the product
REDESIGN_FOLLOWUP.md
PHASE2_RESEARCH_PROMPT.md
CLAUDE_CODE_PROMPT.md
TOURS_CLAUDE_CODE_PROMPT.md
NEXT_STEPS.md
archive/
MEDIA_NOTES.md
media_changes.md
image_urls.tsv

# Research working files — private research data, not for public distribution
Shrines_with_Descriptions.xlsx
Shrines_with_Descriptions_BACKUP_*.xlsx
FULL_SHEET_REPLACEMENT.tsv
MISSING_SHRINES_FOR_SHEET.tsv
STALE_ROWS_TO_UPDATE_IN_SHEET.tsv

# Editor / OS
.DS_Store
.DS_Store?
._*
Thumbs.db
*.swp
*.swo
.idea/
.vscode/settings.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
playwright-report/
test-results/

# TypeScript incremental build info
*.tsbuildinfo

# Vite
*.local

*storybook.log
storybook-static
media/
photo_manifest.tsv
media-source/
data/*.csv
!data/schema_patch.csv
!data/patch_*.csv
# Dated full-sheet restore points (npm run data:snapshot). Committed on purpose:
# the sheet is production and an import replaces it, so the point before an
# import has to be recoverable from a commit, not from someone Downloads folder.
!data/snapshot_*.csv
docs/prompts/PROMPT_post_import.md
PROMPT_post_import.md
*.bak
vite.config.ts.timestamp-*.mjs

# validate_shrines.py run report — regenerated per run, uploaded by CI as an artifact
validation_issues.tsv
pipeline/validation_issues.tsv

# historical run log rescued from the unversioned ~/shrines (see pipeline/legacy-exports/README.md)
!pipeline/legacy-exports/fixes_applied.log
# Throwaway probe scripts (need this repo's node_modules to resolve
# playwright, so they cannot live in a system temp dir)
.scratch/
